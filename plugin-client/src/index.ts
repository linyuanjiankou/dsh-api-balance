/** Host loader entry for the api-balance badge browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the balance badge. */
export function apply(): void {}
